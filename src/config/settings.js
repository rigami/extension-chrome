import { THEME, BG_CHANGE_INTERVAL, BG_TYPE, BG_SELECT_MODE } from "dict";

export default {
    backgrounds: {
        change_interval: BG_CHANGE_INTERVAL.HOURS_1,
        bg_type: BG_TYPE.IMAGE,
        selection_method: BG_SELECT_MODE.SPECIFIC,
        current_bg_link: "default_bg",
    },
    bookmarks: {

    },
    app: {
        theme: THEME.LIGHT,
    },
};